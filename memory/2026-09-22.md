# 2026-09-22

- Reviewed PR #660 from base `d12422e1804f5df6697b59327826fc3e65c4b3d8`, including the remaining wallet-debug working-tree changes; ran all code-deslop passes and an independent second review.
- Kept connector-pinned Wagmi browser sends, local-only preflight, durable one-shot attempt recovery, and wallet-owned fee selection. Added explicit signer pinning, preserved provider rejection causes, and fenced sends after session closure.
- Preserved SVM staged source identity so terminal durable commits suppress stale legacy Actions. Removed obsolete browser-driver shims, aliases, and duplicate capture.
- Validation: 113 focused client, React, proxy, pipeline, widget, and wallet tests; all five app typechecks; library types; lint; frontend boundaries; packed consumers from the trusted base; and all four byte-identical transaction-review visual baselines passed. Portal lint retains two existing mock-export warnings.
- Packed compatibility initially exhausted `/tmp`; rerunning with a disk-backed temporary directory passed. Browser checks used deterministic fixtures, not a real extension or live-funds transaction.
- Merged current `main` (`25dc6548`) after its settings and pool-budget updates landed. Advanced the client patch to `0.9.3` because main already contains `0.9.2`; refreshed CI validates the combined tree and current trusted base.
