# 2026-09-21

- Restored the shared original transaction review below durable transaction cards and removed the temporary service-debug panel.
- Bound durable commit batches and scoped source references to the existing four-phase cards without recreating executable Actions.
- Added explicit browser EVM prepared-send capabilities, durable attempt/hash recovery, and attempt-bound reporting while preserving sign-only, hosted, venue, AA, and SVM routes.
- Regenerated the Agent API contract from the paired Rust OpenAPI source and bumped the client, React runtime, and widget patch versions.
- Applied the previously reviewed pnpm build policy from `a7bb6f86` for the four dependencies required by clean managed installs.
